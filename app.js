const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];const STORE='vmeste-local-events';function go(view){$$('.view').forEach(v=>v.classList.toggle('active',v.dataset.view===view));$$('.nav').forEach(b=>b.classList.toggle('active',b.dataset.go===view));const nav=$('#bottom-nav');if(nav)nav.style.display=['profile','login'].includes(view)?'none':'grid';if(view==='profile')renderProfile();if(view==='calendar')renderEvents();scrollTo(0,0)}$$('.nav').forEach(b=>b.onclick=()=>go(b.dataset.go));$$('.js-profile').forEach(b=>b.onclick=()=>go('profile'));$$('.js-home').forEach(b=>b.onclick=()=>go('home'));function renderProfile(){const root=$('#profile-root');if(!root)return;root.innerHTML='<div class="mark">В</div><span class="ey">ПРОФИЛЬ</span><h1>Тестовый профиль</h1><p class="muted">Первый рабочий контур MVP. Реальную авторизацию подключаем следующим шагом.</p><div class="panel"><button class="primary" id="open-login">Проверить экран входа</button></div>';$('#open-login').onclick=()=>go('login')}const signup=$('#signup-tab'),login=$('#login-tab');if(signup)signup.onclick=()=>{signup.classList.add('active');login.classList.remove('active');$('#name-field').style.display='grid';$('#auth-submit').textContent='Создать аккаунт'};if(login)login.onclick=()=>{login.classList.add('active');signup.classList.remove('active');$('#name-field').style.display='none';$('#auth-submit').textContent='Войти'};const auth=$('#auth-form');if(auth)auth.onsubmit=e=>{e.preventDefault();const s=$('#auth-status');s.hidden=false;s.textContent='Экран работает. Реальную авторизацию подключаем следующим шагом.';s.className='status'};const privateChoice=$('#private-choice');if(privateChoice)privateChoice.onclick=()=>{$('#event-form').hidden=false;$('#event-form').scrollIntoView({behavior:'smooth'})};const form=$('#event-form');if(form)form.onsubmit=e=>{e.preventDefault();const item={title:$('#event-title').value.trim(),date:$('#event-date').value,time:$('#event-time').value,place:$('#event-place').value.trim(),price:+$('#event-price').value||0};const list=JSON.parse(localStorage.getItem(STORE)||'[]');list.push(item);localStorage.setItem(STORE,JSON.stringify(list));const s=$('#event-status');s.hidden=false;s.textContent='Черновик сохранён в тестовом приложении';s.className='status';e.target.reset();$('#event-price').value=0;renderEvents()};function renderEvents(){const root=$('#my-events');if(!root)return;const list=JSON.parse(localStorage.getItem(STORE)||'[]');root.innerHTML=list.length?list.map(e=>'<div class="eventRow"><div><h3>'+escapeHtml(e.title)+'</h3><p class="muted">'+escapeHtml(e.date)+' · '+escapeHtml(e.time)+(e.place?' · '+escapeHtml(e.place):'')+' · '+(e.price?e.price+' ₽':'Бесплатно')+'</p></div><span class="tag">черновик</span></div>').join(''):'<p class="muted">Пока пусто. Создайте первое событие.</p>'}function escapeHtml(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}$$('.repeat').forEach(b=>b.onclick=()=>alert('«Повторить» подключим следующим шагом'));renderProfile();renderEvents();